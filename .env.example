# Root-level env example. Copy to .env (gitignored) and adjust.
# Per-app envs live in apps/api/.env and apps/web/.env.

# Postgres (docker-compose)
POSTGRES_USER=church
POSTGRES_PASSWORD=churchdev
POSTGRES_DB=church_management
POSTGRES_PORT=5432
